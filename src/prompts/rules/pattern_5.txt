CRITICAL RULES:

1. Extract only from the "RC DETAILS OF FOOTING" table.

2. footing_id:
   - Take from TYPE column (F1, F2, etc.)

3. Size:
   - length → L
   - width → B
   - depth → D

4. Reinforcement:
   - Extract from T1, T2, B1, B2 columns.
   - Format is like: T10 AT 200 C/C
   - Extract:
        dia → T10
        spacing → 200 C/C
   - Remove duplicates.
   - Keep format exactly as written (T10, T16 etc).

5. nos:
   - Extract from "NO. OF FOOTING"

6. column_id = null
7. mix = null
8. steel_grade = null
